{
  "name": "chat-mood-meter",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "test:root": "vitest run"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "turbo": "^2.5.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
